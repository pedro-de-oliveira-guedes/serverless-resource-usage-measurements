apiVersion: apps/v1
kind: Deployment
metadata:
  name: pedroguedes-usage-dashboard-deployment
  namespace: pedroguedes
  labels:
    app: pedroguedes-usage-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pedroguedes-usage-dashboard
  template:
    metadata:
      labels:
        app: pedroguedes-usage-dashboard
    spec:
      containers:
      - name: pedroguedes-usage-dashboard
        image: pedroguedes/usage-dashboard:latest
        ports:
        - containerPort: 52055
      imagePullSecrets:
      - name: regcred
